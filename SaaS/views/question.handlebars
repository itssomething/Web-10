<div>
    <h1>{{question.question}}</h1>
    <form action="/question/{{question.id}}" method="post">
        <button type="submit" name="answer" value="yes" >Có</button>
        <button type="submit" name="answer" value="no">Không</button>
    </form>
    <a href="/question">Câu hỏi khác</a>
</div>